{
  "name": "agent-orchestrator",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter @orchestrator/web dev",
    "build": "pnpm --filter @orchestrator/web build",
    "start": "pnpm --filter @orchestrator/web start",
    "lint": "pnpm --filter @orchestrator/web lint",
    "test": "pnpm -r test",
    "test:watch": "pnpm --filter @orchestrator/web test:watch"
  },
  "dependencies": {
    "better-sqlite3": "^12.6.2"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13"
  }
}
